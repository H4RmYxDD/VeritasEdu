<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VeritasEd.Views.TeacherDashboardPage"
             Title="Teacher Dashboard"
             BackgroundColor="{StaticResource BackgroundColor}">
    <ScrollView>
        <VerticalStackLayout Spacing="14">
            <Frame BackgroundColor="{StaticResource PrimaryColor}" CornerRadius="16" Padding="18" HasShadow="True">
                <Label x:Name="WelcomeLabel"
           FontSize="28"
           FontAttributes="Bold"
           TextColor="{StaticResource TextPrimaryColor}"
           HorizontalOptions="Center" />
            </Frame>
            <Frame BackgroundColor="{StaticResource CardColor}" CornerRadius="14" Padding="20" HasShadow="True">
                <VerticalStackLayout Spacing="14">
                    
                    <Label Text="Student"
                           FontAttributes="Bold"
                           FontSize="16"
                           TextColor="{StaticResource AccentColor}" />
                    <Frame BackgroundColor="{StaticResource EntryBackgroundColor}" CornerRadius="10" Padding="4" HasShadow="False">
                        <Picker x:Name="StudentPicker"
        Title="Select Student"
        ItemDisplayBinding="{Binding Username}"
        BackgroundColor="{StaticResource EntryBackgroundColor}"
        TextColor="{StaticResource EntryTextColor}"
        TitleColor="{StaticResource TextSecondaryColor}"
        FontSize="20"
        HeightRequest="54"
        HorizontalOptions="FillAndExpand"
        VerticalOptions="Center"
        Margin="0"/>
                    </Frame>
                    <Entry x:Name="SubjectEntry"
                           Placeholder="Subject"
                           BackgroundColor="{StaticResource EntryBackgroundColor}"
                           TextColor="{StaticResource EntryTextColor}"
                           Margin="0,4"
                           HeightRequest="44"
                           FontSize="16"/>
                    <Entry x:Name="GradeValueEntry"
                           Placeholder="Grade"
                           BackgroundColor="{StaticResource EntryBackgroundColor}"
                           TextColor="{StaticResource EntryTextColor}"
                           Margin="0,4"
                           HeightRequest="44"
                           FontSize="16"/>
                    <Button Text="Save Grade"
                            Clicked="OnAddGradeClicked"
                            BackgroundColor="{StaticResource ButtonColor}"
                            TextColor="{StaticResource ButtonTextColor}"
                            FontAttributes="Bold"
                            CornerRadius="8"
                            HeightRequest="44"/>
                </VerticalStackLayout>
            </Frame>
            <Label Text="All Grades"
                   FontAttributes="Bold"
                   FontSize="20"
                   TextColor="{StaticResource AccentColor}"
                   Margin="0,10,0,0"/>
            <Frame BackgroundColor="{StaticResource CardColor}" CornerRadius="14" Padding="0" HasShadow="True">
                <CollectionView x:Name="GradesCollectionView"
                                EmptyView="No grades available."
                                Margin="0"
                                SelectionMode="None">
                    <CollectionView.Header>
                        <Grid Padding="12,8" BackgroundColor="{StaticResource HeaderBackgroundColor}" ColumnDefinitions="1.2*,2*,1*,Auto,Auto">
                            <Label Text="Student ID" FontAttributes="Bold" TextColor="{StaticResource AccentColor}" Grid.Column="0"/>
                            <Label Text="Subject" FontAttributes="Bold" TextColor="{StaticResource AccentColor}" Grid.Column="1"/>
                            <Label Text="Grade" FontAttributes="Bold" TextColor="{StaticResource AccentColor}" Grid.Column="2"/>
                            <Label Text="" Grid.Column="3"/>
                            <Label Text="" Grid.Column="4"/>
                        </Grid>
                    </CollectionView.Header>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="10,6" ColumnDefinitions="1.2*,2*,1*,Auto,Auto" RowSpacing="0">
                                <Label Text="{Binding Username}" Grid.Column="0" FontSize="16" VerticalOptions="Center" TextColor="{StaticResource TextPrimaryColor}"/>
                                <Label Text="{Binding Subject}" Grid.Column="1" FontSize="16" VerticalOptions="Center" TextColor="{StaticResource TextPrimaryColor}"/>
                                <Label Text="{Binding Value}" Grid.Column="2" FontSize="16" VerticalOptions="Center" TextColor="{StaticResource TextSecondaryColor}"/>
                                <Button Text="Edit"
                                        Grid.Column="3"
                                        Clicked="OnEditGradeClicked"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="#fffde7"
                                        TextColor="#fbc02d"
                                        Padding="8,0"
                                        FontSize="14"
                                        CornerRadius="6"
                                        HeightRequest="32"
                                        WidthRequest="60"/>
                                <Button Text="Delete"
                                        Grid.Column="4"
                                        Clicked="OnDeleteGradeClicked"
                                        CommandParameter="{Binding Id}"
                                        BackgroundColor="#ffebee"
                                        TextColor="#e53935"
                                        Padding="8,0"
                                        FontSize="14"
                                        CornerRadius="6"
                                        HeightRequest="32"
                                        WidthRequest="70"/>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>